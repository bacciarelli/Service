{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modelmachine</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modelMachine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ modelMachine.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('modelmachine_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('modelmachine_edit', { 'id': modelMachine.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
