{% extends 'base.html.twig' %}

{% block body %}
    <h1>Repair description</h1>
    {{machine.type}}
    {{ form_start(repair_form) }}
        {{ form_widget(repair_form) }}
        <input type="submit" value="Add repair description" />
    {{ form_end(repair_form) }}
   
    <br>
    <br>

    <ul>
        <li>
            <a href="{{ path('machine_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
